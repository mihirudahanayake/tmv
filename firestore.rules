rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow only signed-in users to read and write anything
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
