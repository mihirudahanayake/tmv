rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMP: Allow all reads/writes for local development
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
